function add (first, second){
    const result = first + second;
    if (result > 5){
        const mode = 'sexy';
        console.log(mode);
    }


    return result;
}

const number = add (20, 21);

