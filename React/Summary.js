/*
1. variable (let,const,var) three types variable
2.condition(<, >, ===, !== , <=, =>) && ||, if else

3.Array: Declare, Length, index, push,pop , indexOf

4. for loop

5.funciton return parameter
 6. object property, including array, object



 ES6
 1. template string $(``)
2.Spead (...) Copy an array then add  a new array
3.filter - use filter to remove an element from an array
4.arraow function
 - no parameter()
 -single parameter
 -multiple parameters ()
 function parameter default value
 optional chaining(?,)
-multi line
-destrucring : object destrucuring array desstruckt
-object shorthand
-Function parameter default value
-.........

Browser API
1.local storage session storage
local API
history API
fetch

other 
1. array: map, forEach, filter, find
ternary operator
logical and logical or && "" localStorage.setItem(id, value) || &&
JSON (stringify, parse)



*/

const x = 50;
const y = 100;
const obj = {
    x: x,
    y: y
};

// shortcut
const obj1 = {x, Y}

const name = 'solimullah';
const area = 'Shaka'