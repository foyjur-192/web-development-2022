const multilineNew = `This is an amazing path to get sucess in our life time`
const friends = ['abul', 'babul','kabul', 'sabul'];
const new1 = `<h3 class="friend-name">Friend-${friends.length}</h3>`;
// console.log(new1);

//Template string
const first = 'Mamun';
const last = 'Chowdhury';
const fullNew = `${first} ${last}. Has money ${friends.length * 500}.He lives in Dhaka`;
console.log(fullNew);
