let balance = 1240;
balance = 1340;
const userName = 'janpahi potoas';
const weTogether = 'ami' + userName;
console.log(weTogether);
//Reassign is not allowed in const
//ES6
const numbers = [45, 23, 89, 60];
//we cannot change but push
numbers.push(555);
numbers[1] = 333;


for(let i = 0; i < numbers.length; ++){
    const number(20,10);
}

const student = {roll: 101, name:'mofiz', job: 'intern'};
student.name = 'MOfazzol';
//We cannot reassign the name